#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../eslint-pre-commit-hook/src/main.js" "$@"
else 
  exec node  "$basedir/../eslint-pre-commit-hook/src/main.js" "$@"
fi
